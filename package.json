{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel serve -d dist src/index.html",
    "build": "parcel build -d build --public-url /Loldex/ src/index.html",
    "deploy": "gh-pages -d build"
  }
}
